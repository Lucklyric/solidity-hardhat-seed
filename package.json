{
  "name": "solidity-hardhat-seed",
  "version": "1.0.0",
  "description": "Solidity hardhat seed template",
  "main": "index.js",
  "author": "Alvin(Xinyao) Sun <lucklyric@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/contracts",
    "packages/sdk"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "lint:fix": "yarn workspaces run lint:fix",
    "format": "yarn workspaces run format",
    "format:fix": "yarn workspaces run format:fix",
    "test": "yarn workspaces run test",
    "test:cov": "yarn workspaces run test:cov"
  },
  "dependencies": {
    "husky": "^7.0.2",
    "lint-staged": "^11.1.2"
  }
}
